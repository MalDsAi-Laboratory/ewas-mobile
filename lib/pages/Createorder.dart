import 'package:flutter/material.dart';

class CreateOrderPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Create Order")),
      body: Center(
        child: Text("This is the Create Order Page"),
      ),
    );
  }
}
